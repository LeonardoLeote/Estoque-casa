name: estoque_casa
description: Controle de estoque doméstico compartilhado.
publish_to: none
version: 1.1.0+2

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.17.2
  http: ^1.6.0
  image_picker: ^1.2.3
  shared_preferences: ^2.5.5
  flutter_local_notifications: ^22.3.1
  intl: ^0.20.2
  flutter_slidable: ^4.0.3
  share_plus: ^13.3.0
  package_info_plus: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
